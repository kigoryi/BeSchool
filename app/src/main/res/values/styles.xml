<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/screenColor</item>
        <item name="colorPrimaryDark">@color/screenColor</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- style for the selection option activity-->
    <style name="ApTheme" parent="Theme.AppCompat.Light">

        <item name="colorPrimary">@color/screenColor</item>
        <item name="colorPrimaryDark">@color/screenColor</item>
        <item name="colorAccent">@color/colorAccent</item>

    </style>



</resources>
